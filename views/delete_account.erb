<!DOCTYPE html>
<html>
    <head>
        <meta charset='UTF-8' />
        <title>Delete Account</title>
    </head>
    <body>
        <% @account.all do |i| %>
        
        <a href="/home/">Home</a>
        <a href="/accounts/<%= i.pk %>">Account</a>
        <a href="/dashboard/<%= i.pk %>">Dashboard</a>
        <a href="/activities/<%= i.pk %>">Activities</a>
        <a href="/books/<%= i.pk %>">Books</a>
        <a href="/subjects/<%= i.pk %>">Subjects</a>
        
        <h1>Delete Account</h1>
        
        <h3><%= i.acct_first_name %>, deleting your account will delete all students, activities, books, subjects and personal information associated with your account.</h3>
        
        <h3>Are you sure you want to delete everything and close your account?</h3>
        
        <form method="post" action="/accounts/delete/<%= i.pk %>">
            <table>   
                <tr>
                    <td><label for="submit">Delete Account</label></td>
                    <td><input type="submit"></td>
                </tr>
            </table>
        </form>
        <% end %>
    </body>
</html>