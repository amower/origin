<!DOCTYPE html>
<html>
    <head>
        <meta charset='UTF-8' />
        <title>Delete Account</title>
    </head>
    <body>
        <% @account.all do |row| %>
        
        <a href="/home/">Home</a>
        <a href="/accounts/<%= row[:account_id] %>">Account</a>
        <a href="/dashboard/<%= row[:account_id] %>">Dashboard</a>
        <a href="/activities/<%= row[:account_id] %>">Activities</a>
        <a href="/books/<%= row[:account_id] %>">Books</a>
        <a href="/subjects/<%= row[:account_id] %>">Subjects</a>
        
        <h1>Delete Account</h1>
        
        <h3><%= row[:acct_first_name] %>, deleting your account will delete all students, activities, books, subjects and personal information associated with your account.</h3>
        
        <h3>Are you sure you want to delete everything and close your account?</h3>
        
        <form method="post" action="/accounts/delete/<%= row[:account_id] %>">
            <table>   
                <tr>
                    <td><label for="submit">Delete Account</label></td>
                    <td><input type="submit"></td>
                </tr>
            </table>
        </form>
        <% end %>
    </body>
</html>