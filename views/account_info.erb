<!DOCTYPE html>
<html>
    <head>
        <meta charset='UTF-8' />
        <% @account.all do |a| %>
        <title><%= a.acct_last_name %> Portfolio</title>
    </head>
    <body>
        <a href="/home/">Home</a>
        <!--Sequel model pk method retrieves record's primary key value (in this case the account_id)-->
        <a href="/accounts/<%= a.pk %>">Account</a>
        <a href="/dashboard/<%= a.pk %>">Dashboard</a>
        <a href="/activities/<%= a.pk %>">Activities</a>
        <a href="/books/<%= a.pk %>">Books</a>
        <a href="/subjects/<%= a.pk %>">Subjects</a>
        
        <!--You can read record values as object attributes if attribute names are valid columns in the dataset (i.e. row.account_id). If attribute names are not valid columns, you can access values with [] (i.e. row[:account_id]).-->
        <h1><%= a.acct_first_name %> <%= a.acct_last_name %> Account Info</h1>
        
        <table>
            <tr>
                <td><strong>Email:</strong></td>
                <td><%= a.email %></td>
            </tr>
            <tr>
                <td><strong>Zipcode:</strong></td>
                <td><%= a.zipcode %></td>
            </tr>
            <tr>
                <td><strong>Join Date:</strong></td>
                <td><%= a.join_date.month %>-<%= a.join_date.day %>-<%= a.join_date.year %></td>
            </tr>
            <tr>
                <td><strong>Password:</strong></td>
                <td><%= a.password %></td>
            </tr>
        </table>
        
        <a href="/accounts/update/<%= a.pk %>">Edit Account</a><br>
        <a href="/accounts/change/<%= a.pk %>">Delete Account</a>
        
        <h3>Student(s) Enrolled:</h3>
        
        <ol>
        <% @students.each do |s| %>
            <li>
                <p><a href="/students/<%= s.account_id %>/<%= s.pk %>"><%= s.stud_first_name %></a> (<a href="/students/update/<%= s.pk %>">Edit</a>) (<a href="/students/change/<%= s.account_id %>/<%= s.pk %>">Delete</a>)
                </p>
            </li>
        <% end %><!--End students loop-->
        </ol>
        
        <a href="/students/new/<%= a.pk %>">Add New Student</a><br>
        
        <h2>
            <a href="/dashboard/<%= a.pk %>">Go to My Dashboard</a>
        </h2>
        
        <% end %><!--End account data access-->
        
    </body>
</html>