<!DOCTYPE html>
<html>
    <head>
        <meta charset='UTF-8' />
        <title>Delete Subject</title>
    </head>
    <body>
        <% @subject.all do |row| %>
        
        <a href="/home/">Home</a>
        <a href="/accounts/<%= row[:account_id] %>">Account</a>
        <a href="/dashboard/<%= row[:account_id] %>">Dashboard</a>
        <a href="/activities/<%= row[:account_id] %>">Activities</a>
        <a href="/books/<%= row[:account_id] %>">Books</a>
        <a href="/subjects/<%= row[:account_id] %>">Subjects</a>
        
        <h1>Delete Subject</h1>
        
        <h3>Deleting <%= row[:subject_name] %> from your account will delete the subject from all activities and books.</h3>
        
        <h3>Are you sure you want to delete <%= row[:subject_name] %>?</h3>
        
        <form method="post" action="/subjects/delete/<%= row[:account_id] %>/<%= row[:subject_id] %>">
            <table>   
                <tr>
                    <td><label for="submit">Delete Subject</label></td>
                    <td><input type="submit"></td>
                </tr>
            </table>
        </form>
        
        <% end %>
    </body>
</html>